package evolution_simulator.modes

enum class MapMode {
    Globe,
    Portal;
}

enum class PlantGrowthMode {
    EquatorialForest,
    ProLife;
}

enum class MutationMode {
    HardMutation,
    SoftMutation;
}